[Unit]
Description=Yet Another Develop Deployment Tool
Requires=network-online.target
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/refine
Restart=always

[Install]
WantedBy=default.target
